model:
  name_or_path: "microsoft/deberta-v3-large"
  hidden_dropout_prob: 0.1
  attention_probs_dropout_prob: 0.1
  max_position_embeddings: 512

training:
  batch_size: 1             # Reduced to 1 to prevent CUDA Out Of Memory
  gradient_accumulation_steps: 32  # Increased to maintain effective batch size of 32
  learning_rate: 1e-5
  weight_decay: 0.01
  num_train_epochs: 3
  warmup_ratio: 0.1
  seed: 42
  fp16: true                # Crucial for GPU speedup

data:
  train_path: "/kaggle/working/sage/data/processed/train_10k.parquet"
  test_path: "/kaggle/working/sage/data/processed/test_1k.parquet"
